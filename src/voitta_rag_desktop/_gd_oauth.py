"""Built-in Google Drive OAuth client (Desktop-app type).

Placeholder — desktop/build_app.sh regenerates this module at build time
from the build machine's VOITTA_GD_BUILTIN_CLIENT_ID/SECRET env vars.
Empty values mean the built-in "Sign in (no setup)" Drive tab is simply
not offered. An installed-app client secret is not confidential (Google's
documented stance) but is still never sent to the SPA.
"""

CLIENT_ID = ""
CLIENT_SECRET = ""
